function matrix = pnorm(frequencies,phases,pnorm)
    matrix = [];
    for p1 = 1:lengh(frequencies)
       for p2 = 1:lengh(frequencies)
            d = 
            matrix = [matrix,]
        end
    end
    return;
end
